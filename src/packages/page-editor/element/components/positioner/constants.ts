import {ShapeType} from './types';

export const initialShapeData = {
    type: ShapeType.SQUARE,
    position: {
        rowStart: 1,
        rowEnd: 5,
        columnStart: 2,
        columnEnd: 6,
    },
};
